// Tailwind CSS v4 - Configuration moved to globals.css
// This file is kept for compatibility but most config is now in CSS
export default {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
}
